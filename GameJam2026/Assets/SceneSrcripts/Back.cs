using UnityEngine;

public class Back : MonoBehaviour
//{
   // [Header("Какой Акт новеллы запустить после завершения 3D?")]
    //public DialogueData nextStoryAct; // Сюда перетягиваем Act3_After3D_1.asset

    // Вызывается, когда игрок разгадал загадку / нажал на выход
    //public void Finish3DScene()
    {
        // 1. Выключаем 3D режим
       // GameManager.Instance.Disable3DMode();

        // 2. Возвращаем новеллу и передаем ей следующий Акт из папки
       // DialogueManager.Instance.StartDialogue(nextStoryAct);
    }
//}
